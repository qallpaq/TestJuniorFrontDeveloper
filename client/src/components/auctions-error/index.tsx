import { Typography } from '@mui/material';
import React from 'react';

const AuctionsError = () => {
  return <Typography>По вашему запросу ничего не найдено</Typography>;
};

export default AuctionsError;
